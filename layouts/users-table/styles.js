import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  tableHead: {
    fontWeight: 700,
    fontSize: "16px",
  },
  actions : {
    display: 'flex',
    // justifyContent: 'space-evenly'
  }
});
